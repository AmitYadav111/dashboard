// Theme constants - single source of truth for theme names
export const THEMES = {
  DEFAULT: "default",
  CLIENT_A: "clientA",
  CLIENT_B: "clientB",
};

// List of valid theme names
export const VALID_THEMES = Object.values(THEMES);

// Default theme fallback
export const DEFAULT_THEME = THEMES.DEFAULT;

// Theme class prefix
export const THEME_CLASS_PREFIX = "theme-";
